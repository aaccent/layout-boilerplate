doctype html
html(lang='ru')
    head
        block head
            meta(name="viewport" content="width=device-width, initial-scale=1.0")

            link(href=require('scss/styles.scss') rel="stylesheet")
    body
        include general/header
        include popups/popup

        div.main-wrapper
            main.main
                block main

            include general/footer

        block scripts
            script(src=require('js/main.ts'))